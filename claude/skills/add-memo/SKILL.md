---
name: add-memo
description: >-
  個人メモリポジトリ（~/development/suimenkathemove/memos）に、メモしたい内容を追加・更新する。
  「メモに追加して」「メモって」「memos に書いて」などと言われたときに使う。
---

# memos リポジトリにメモを追加する

対象リポジトリ: `~/development/suimenkathemove/memos`

- `memos/` : 技術トピック別のメモ本体（このスキルの主な追加先）。
  公開リポジトリにコミットされるので、公開してよい内容だけ。
- `memos_private/` : 非公開リポジトリの submodule。
  社名・案件名・認証情報など機微な内容はここへ（非公開リポジトリにコミットされる）。
- `_shared/` : todo・タスク・プロンプトなど、**技術メモではない作業用のもの**。
判断の順番:

1. 残す価値のある技術メモか → いいえ なら `_shared/`。
3. 消えても困らない一時ファイルだけ `_local/`。

迷ったら `memos/` 配下に置く。
機微な内容（社名・案件名・認証情報など）が含まれる場合は `memos_private/` に置くか、ユーザーにどちらへ入れるか確認する。

## 手順

