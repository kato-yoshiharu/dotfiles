---
name: add-memo
description: >-
  個人メモリポジトリ（~/development/suimenkathemove/memos）に、メモしたい内容を追加・更新する。
  「メモに追加して」「メモって」「memos に書いて」などと言われたときに使う。
---

# memos リポジトリにメモを追加する

対象リポジトリ: `~/development/suimenkathemove/memos`

- `memos/` : 技術トピック別のメモ本体（このスキルの主な追加先）。
  公開リポジトリにコミットされるので、公開してよい内容だけ。
- `memos_private/` : 非公開リポジトリの submodule。
  業務先の社名・案件名を含むメモなど、公開リポジトリに置けない内容はここへ。
- `_shared/` : todo・タスク・プロンプトなど、**技術メモではない作業用のもの**。
  グローバル gitignore に入っているためコミットされない。
- `_local/` : このマシンだけの一時ファイル・スクラッチ。
  同じくグローバル gitignore に入っておりコミットされない。

判断の順番:

1. 残す価値のある技術メモか → いいえ なら `_shared/`。
2. 公開リポジトリに置けない内容（案件の内容）を含むか →
   含むなら `memos_private/`、
   含まないなら `memos/`。迷ったらユーザーに確認する。
3. 消えても困らない一時ファイルだけ `_local/`。

`_shared/` と `_local/` はコミットされないので、**残したい知見をここに書かない**。

## 手順

1. 追加先を決める。
   - まず `ls memos/` と `grep -ril <キーワード> memos/` で**既存の該当メモを探す**。
   - 既存メモがあれば新規作成せず、そこに追記する（適切なセクション）。
   - トピックがディレクトリ（例: `memos/git/`, `memos/typescript/`）なら、その中の
     適切なファイルに追記するか、新しい `<topic>.md` を作る。
   - 該当がなければ `memos/<topic>.md` を新規作成する。ファイル名は小文字ケバブケース。
2. 内容を書く。既存メモの書き方に合わせる（下記ルール参照）。
3. `pnpm markdownlint`（または `npx markdownlint-cli2 '<追加したファイル>'`）で lint を通す。
4. コミットは行わない。

## 書き方のルール

## やらないこと

- ユーザーが言っていない内容を推測で膨らませない。書くのは実際に得られた知見だけ。
- 既存メモの体裁の一括修正など、依頼されていない変更をしない。
